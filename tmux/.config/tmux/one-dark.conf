set -g @thm_bg "#282c34"
set -g @thm_fg "#abb2bf"
set -g @thm_rosewater "#ebd09c"  # Closest match to soft, warm tones
set -g @thm_flamingo "#d19a66"   # A warm orange hue
set -g @thm_pink "#c678dd"       # Purple-pink tone
set -g @thm_mauve "#8a3fa0"      # Dark purple
set -g @thm_red "#e86671"        # Primary red
set -g @thm_maroon "#993939"     # Dark red for deeper tones
set -g @thm_peach "#d19a66"      # Orange for peach tones
set -g @thm_yellow "#e5c07b"     # Primary yellow
set -g @thm_green "#98c379"      # Primary green
set -g @thm_teal "#56b6c2"       # Cyan leaning towards teal
set -g @thm_sky "#73b8f1"        # Light sky blue
set -g @thm_sapphire "#61afef"   # Standard blue
set -g @thm_blue "#4d84bf"       # Darker blue variant
set -g @thm_lavender "#c678dd"   # Purple/lavender tone
set -g @thm_subtext_1 "#848b98"  # Light grey for subtle text
set -g @thm_subtext_0 "#5c6370"  # Grey for regular subtext
set -g @thm_overlay_2 "#393f4a"  # Dark overlay for depth
set -g @thm_overlay_1 "#31353f"  # Mid-tone overlay
set -g @thm_overlay_0 "#282c34"  # Base overlay
set -g @thm_surface_2 "#3b3f4c"  # Slightly lighter surface
set -g @thm_surface_1 "#31353f"  # Mid-tone surface
set -g @thm_surface_0 "#21252b"  # Dark surface layer
set -g @thm_mantle "#181a1f"     # Mantle background
set -g @thm_crust "#141518"      # Darkest background


